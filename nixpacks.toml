# Explicit Nixpacks config - use npm install (npm ci requires synced lock file)
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "cd backend && NODE_ENV=production node server.js"
